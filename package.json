{
  "name": "@aicopycoders/exodus",
  "version": "2026.7.200-beta.9",
  "description": "CLI + Claude Code skills for the Copycoders Exodus ad pipeline",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/aicopycoders/exodus.git"
  },
  "homepage": "https://github.com/aicopycoders/exodus#readme",
  "type": "module",
  "bin": {
    "exodus": "dist/bin/exodus.js"
  },
  "files": [
    "dist",
    "assets"
  ],
  "engines": {
    "node": ">=20"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "bundle-assets": "node scripts/bundle-assets.mjs",
    "prepack": "npm run build && npm run bundle-assets",
    "dev": "tsx bin/exodus.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.25.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
